import ComingCard from "../ComingCard";

export const dynamic = "force-dynamic";

export default function ProjectPage() {
  return (
    <ComingCard
      title="My project"
      body="Once your painting is underway, this is where you'll watch it happen — photos as they're taken, what's done and what's next, and the people looking after your home, day by day."
    />
  );
}
