import ComingCard from "../ComingCard";

export const dynamic = "force-dynamic";

export default function MessagesPage() {
  return (
    <ComingCard
      title="Messages"
      body="Everything we send you will be kept here, next to your project — so nothing gets lost and 'where did they say that?' always has one answer."
    />
  );
}
