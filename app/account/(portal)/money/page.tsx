import ComingCard from "../ComingCard";

export const dynamic = "force-dynamic";

export default function MoneyPage() {
  return (
    <ComingCard
      title="Money"
      body="Your invoices and receipts will live here — every amount in plain figures with GST shown, and a PDF you can download whenever you need it. If we've emailed you an invoice, the link in that email always works too."
    />
  );
}
