# Estimator v2 — progress ledger

**Read this first in every session. Update it last.** One row per chunk from `estimator-v2-delta-and-plan.md`. The row is the truth about what has shipped; the briefs are the truth about what to build. If they disagree, say so in the session before writing code.

## Preflight — paste at the start of every session

    Read docs/briefs/estimator-v2-progress.md. Take the last row whose status
    is not TODO. Then, before touching code, report in one block:
    1. git log --oneline <that row's SHA>..origin/main — what has landed since.
    2. supabase/migrations/ files newer than the row's last migration, and
       whether each has a row in public._prod_migrations (output the SELECT
       for Tom if you cannot read production).
    3. Settings keys the next chunk expects, and which already exist.
    4. Any open branch touching the files the next chunk names.
    5. The next chunk to run, and anything in its block that the repo has
       already done — shrink the block accordingly and say what you removed.
    6. Open design/reference/estimator-journey-v2.html and read every screen
       the chunk names (by id) and its ? drawer. Confirm you are building to
       the prototype, not to an earlier description of it.
    Wait for confirmation before starting the chunk.

## Postflight — the last act of every session

    Add the row: chunk, status DONE / PARTIAL / BLOCKED, date, merge SHA,
    migrations written (and whether Tom has confirmed them live), Settings
    keys seeded, unit count before → after, open ⚑s touched, and one line of
    "what the next session should know". Commit the ledger in the same PR.

## The ledger

| Chunk | What | Status | Date | Merge SHA | Migrations written · live? | Settings seeded | Unit tests before → after | ⚑ touched | Next session should know |
|---|---|---|---|---|---|---|---|---|---|
| — | Baseline: audit at `a1b28fc` | REF | 11 Sep 2026 | `a1b28fc` | production ledger absent | — | 2066 passing | — | 9.1 and 9.2 open; `wizard_drafts` has no version column; `commercial-pricing-strategy.md` untracked |
| C0 | Ledger, stragglers, the missing brief | DONE | 11 Sep 2026 | _(PR pending)_ | `20270135000000_prod_migrations` · **still awaits Tom on PROD — the 12 Sep paste came from the TEST project (see C0 note)** | — | 2066 → 2076 | A, D, E, G ruled | Backfill INSERT for all 180 files is in the PR body — Tom prunes and pastes it ONCE, then confirms. 15 migrations since 6 Sep still unconfirmed on prod; C1 does not start until they are. Prototype on main was the 9 Sep file — replaced with v2.5, which is the one carrying `s-both`, `sheet-save`, the five `s-com-*` screens and the SEG/BRIEF seeds. Seed scripts still write to production by construction — see the parking lot. **⚑ TOM'S `_prod_migrations` PASTE, 12 Sep, WAS FROM THE TEST PROJECT.** The TEST project (`qarfyjrz…`) holds exactly the same 44 rows with `applied_at` values identical to the microsecond — read live from it the same day — so the SELECT ran there, not on `llmrvgde…`. Production's `_prod_migrations` state is therefore UNKNOWN, and it may not exist there at all. Tom, 12 Sep: 137 and 139 'may have been pasted incorrectly' — they have no row on test either, so wherever they went, no self-register fired. The reads of prod that would settle this are classifier-blocked from the AI's session; the checks are Tom's, listed below. What the 44 rows on TEST say:  The backfill covers exactly 40 files, `20260813000000_initial_schema` → `20260913000000_step3_shared_scope` (all stamped 2026-09-10 22:57 UTC), plus the four self-registering rows `20270132`, `20270135`, `20270136`, `20270138`. **14 of the 15 post-6-Sep files have NO row:** `20270120`–`20270131` (the 7 Sep CRM v2 set + cement sheet + batch + agent tone + gcal scopes), `20270133`, `20270134`. Only `20270132` does. Also absent: **`20270137` and `20270139`**, which SELF-REGISTER and which the C5/C6 rows call LIVE on prod — a self-registering file with no row was not run on the project that answered this query. And 126 files between `20260914` and `20270119` have no row either — the backfill INSERT was never in PR #62's body (it is empty), so the 40-row list came from somewhere else and stops where it stops. Question (1) is answered: it was NOT. Still open: (2) were `20270137` and `20270139` pasted on prod, or on the test project on 11 Sep; (3) is the 40-file backfill deliberate (only those are live) or truncated. The 14 missing post-6-Sep files are listed in the parking lot with what each creates, so a paste can be checked table by table. |
| C1 | One ladder | DONE | 11 Sep 2026 | _(PR pending)_ | `20270132000000_wizard_policy_v2_keys` (rebased from branch, now self-registers) · **still awaits Tom on PROD — has a row on the TEST project only (12 Sep)** | `wizard_bands`, `wizard_policy` — NOT `wizard_rewards` | 2076 → 2096 | A(no), D, G | Ruling G applied: the branch shipped bronze/silver/gold + a `wizard_rewards` key (paint upgrade, first pick of start dates, `goldSkipVisit`). All stripped; tiers are now guide/detailed/confirmed. **The tier-chip CSS class IS the tier value** (`ScopeEditor.tsx:661`) — `wizard.css` had to be renamed with it or the chip goes unstyled silently. 9.5 fixed in `policy.ts`: both segment-driven gates (strata, healthcare) join `softForActor`; the four physical gates stay hard. The `softReasons` copy is defensive only — the alias never escaped, and no test can prove it. e2e green on the C1 stack, but `wizard_public.enabled` is FALSE there, so every customer-journey spec fails on the holding page until it is turned on — likely one of the CI-red classes for C17. |
| C2 | `requires_site_check` decided once | DONE | 11 Sep 2026 | _(PR pending)_ | none — but a DATA CORRECTION for proving rows is in the PR body | — | 2096 → 2115 | — | `requiresSiteCheck({state, stored})` in ladder.ts is the only derivation; FOUR callers share it, not the two the block named — `wizard-edit` and `scope-tools` also read the column and would still have disagreed on a pre-C2 row. `effectiveState` not `state` at submit (a failed defect read must not count as a photo). Snapshot gained `requiresSiteCheck`; its ABSENCE marks a row from the affected window. Exterior rows are NOT corrupted — `exterior_signoff` forced walkthrough true either way. Also carries the C1 migration-header fix, which PR #64 merged too early to include. **PROVING ROWS CHECKED ON PROD 11 Sep: 0 needed correction — step 2 was never run.** 6,668 snapshots, 6,481 from the affected window, 0 contradictions. Why: `at_or_above_the_bar = 0` — NOT ONE of 6,668 estimates reached 90% accuracy at submit, so `accuracy_below_bar` forced the walkthrough every time and the wrong flag never changed an outcome. The bug was real and never fired. **Tom, 11 Sep: the wizard has never been live — all 6,668 are TEST estimates**, so the accuracy distribution reflects the drivers, not customers, and nothing about the real-world bar can be inferred from it (an earlier note here did infer it; retracted in the parking lot). C2 was preventive: it landed before exposure, which is the good outcome. ⚑ The proving window has NOT started — its baseline is driver output; see the parking lot for C17. |
| C3 | Draft versioning, one server truth | DONE | 11 Sep 2026 | _(PR pending)_ | `20270136000000_wizard_drafts_version` · **LIVE on prod + C1** | — | 2115 → 2133 | A1b CLOSED | Two-tab e2e GREEN (3 specs, run 4× plus a 7-spec regression batch). It caught TWO bugs reasoning had missed: (1) the localStorage cache is written 400 ms after a keystroke but the server confirms at 2.5 s+, so its version stamp always lagged and `pickResume` wrongly read 'browser is behind' — the cache now re-stamps on confirmation; (2) `decodeResume` rebuilds the record field by field, so `version` and `lastScreen` were silently dropped on the way back in. Also: the resume used to INFER the screen via `entryFromState`, which keys on `state.quickLook` — a key that only exists once the customer ANSWERS a quick-look question, so anyone who typed an address and left came back to the page set. `last_screen` now decides. Invariant: ONE writer of `state`, pinned by a source-reading test. |
| C4 | Outside + Commercial; exterior finish line | DONE | 11 Sep 2026 | _(PR pending)_ | none | — | 2133 → 2147 | — | **Phase 0 complete.** Closes 9.3(a)(b)(c) and 9.4. The commercial hand-off now keys on `quickStep === "place"`, not every Continue. Commercial + outside/both → the `handoff` outcome with `flushDraft` so the lead survives the tab closing; commercial + INSIDE still goes into the page set for the segment question — deliberate. The exterior finish line reuses the SAME `Finish` and `summaryRows` (a second summary would be a second opinion); `SummaryInput.sides` and `finishOptions(kind)` are new. ⚑ C14 REPLACES the 9.3(c) stop-gap with the exterior brief. All four fixes mutation-checked; 16/16 e2e green in a regression batch. |
| C5 | Qualified-lead pack and the queue | DONE | 11 Sep 2026 | _(PR pending)_ | `20270137000000_confirmation_requests` · **LIVE on prod + C1** | `profiles.patch_postcodes` (empty = never auto-assigned) | 2147 → 2182 | I | The block reads as greenfield; it is NOT. ⚑7's desk check already had the pack, the suggested action AND a queue — keyed off `builder_state.prepPack.kind='desk_check'`. Tom ruled option 1: MIGRATE the queue onto the table, do not add a second one. `prepPack` keeps its own job (visit capture-verify data). The migration BACKFILLS every marker so nothing was lost. `buildDeskCheckItems` had ZERO tests before the rewrite — 12 now. ⚑ The e2e found two real things: the row creation THREW if the pack could not be frozen (skipping the promise to protect a nice-to-have), and the send path was uncovered because the scope→finish CTA had no testid and its label changes with the ladder. Both fixed; `scope-finalise` is now a stable handle C7 will need when it renames that button again. |
| C6 | Fix, ask, visit | DONE | 11 Sep 2026 | `16d9bd5` + this | `20270138000000_fix_price_and_turnaround` · **LIVE on prod + C1**; `20270139000000_invoice_fk_indexes` · **LIVE on prod** (out of scope — see below) | `confirmation_turnaround` = `{"hours": 8, "words": "usually by the next working day"}` | 2182 → 2212 | 7, 8 | Three actions, one route: `app/api/confirmations/[id]/route.ts` is staff-only, idempotent (a repeat of the producing action returns 200 `repeated:true`) and writes conditionally with `.eq("status", row.status)`, so two estimators tapping at once cannot both win. `canAct` in `lib/wizard/confirmation-actions.ts` holds the one policy fact: **`fixed` is terminal, `question_asked` is not** (a customer can be asked twice). ⚑8: fixing a price takes the engine's **central** estimate, never what the estimator typed — an entered figure may sit outside the range, and the ladder, not a person, decides what we can stand behind. The fix path writes `properties.measured_tree` + `measured_at` (`20270138` brought that column forward from C15) so the next estimate for the same address starts measured. ⚑ `isOverdue` was WRONG first: it used `getHours()`/`getDay()`, which answer in the runtime's zone — green under `TZ=Australia/Melbourne`, wrong on a UTC runner. It now goes through `addBusinessHours` (Melbourne-aware, `OPEN_HOUR = 9` — one of my own tests had assumed 8). The overdue surface is NOT a new card: `buildDeskCheckItems` now sets `dueAt` to `addBusinessHours(created, turnaround.hours)` — **the promise we actually made** — so the queue's existing overdue bucket and priority boost carry it, and changing the Settings row moves both the date and the customer-facing wording. A weekend cannot make a Friday request late. Mutation-checked (reverting `dueAt` turns 2 of 17 red). ⚑ Out of scope but shipped: `20270139` indexes `crm_events.invoice_id` and `messages.invoice_id` — two unindexed FKs were why deleting one invoice timed out. Applied on prod; the TEST project still holds 40,313 `E2E-LP-*` invoices (parked — see the parking lot). `destroyLoopFixture` now verifies **every** delete and names the table that refused, instead of returning clean. |
| C7 | Self-sign, the hold, the hand-off | DONE | 11 Sep 2026 | _(PR pending)_ | **none, as planned** | `wizard_hold_days` — **no migration**: `holdDaysFromSettings` defaults to 60 when the row is absent, and Settings → Estimates → tiers now edits it beside the caps that decide who may fix a price | 2242 → 2242 | 8, J | **⚑8 was not being honoured, and the fix is the centrepiece.** Three places computed the fixed price as `(rangeLo + rangeHi) / 2` and called it the central estimate. It is not: `rangeFromTotal` floors the low end and ceils the high one to $10, so their average drifts from the figure the engine actually produced — by a few dollars usually, more as the band widens, on the one number nobody may correct afterwards. `CustomerPayload.centralCents` now carries the engine's own `totals.totalCents` and nothing downstream does arithmetic on money. ⚑ THE FIRST CUT OF THAT LEAKED: `adversarial.test.ts` asserts the exact point price never reaches a customer payload (the whole premise is a range, not a number) and it caught it immediately. The field is now `null` unless `canAccept` — the one case where the figure is already printed on the door the customer is about to tap. Both halves are pinned, so it cannot be 'fixed' by deleting the field or by quietly ungating it. `mayFixOnline(decision)` is a named export of `ladder.ts`, called by `ladderFor` AND by the route at the moment of the tap — the route had its own three-clause copy, which is the exact drift C1/C2 spent themselves removing. The route evaluates the ladder ONCE (memoised) and the customer payload reuses it, so a fixed price and the payload reporting it cannot disagree. A refused fix is **200, not 4xx**: a stale screen is nobody's fault, so the server makes the ordinary confirmation request instead and the page says so kindly. Idempotent — a second tap returns the first fix rather than writing a second price and re-stamping the hold (`confirmation_requests_open_per_estimate` excludes terminal statuses, so nothing else stops that). The hold LIVES on `estimates.valid_until` — the column the daily lapse sweep already reads; a hold recorded only on the confirmation row would be a promise nothing enforced. `holdUntil` counts Melbourne's calendar day, and the repo's own offset guard (`lib/workorder/boundary.test.ts`) caught the `+10:00` I hardcoded in the date the customer reads — which would have rendered a day early right through daylight saving. Because the suite runs under TZ=Australia/Melbourne a naive date test cannot fail, so the real guard is a source-reading one: no runtime-local date accessor may appear in `confirmation-actions.ts`. v2.4: `sendToLabel` renames the CTA to **Send to &lt;first name&gt;** in all three surfaces (scope editor, sides editor, assistant pane) — resolved from `profiles.patch_postcodes` then Settings, and it REFUSES to invent a name: no record, old label. `estimatorLine` introduces the person on the hand-off in the customer's own suburb, not a list of postcodes, and never guesses a pronoun. ⚑⚑ **THE E2E COULD NOT BE RUN — the C1 test project's auth write path is hung.** `POST /auth/v1/signup` and `POST /auth/v1/token?grant_type=password` both time out (504, then no response at all at 60s) while `/auth/v1/health` returns 200 and PostgREST returns 200. Every customer-journey spec begins with `signInAnonymously`, so NONE of them can start — this is also the likeliest cause of the CI e2e red. The three specs are written and committed (`e2e/customer-journey/self-sign.spec.ts`), unrun. C7 is NOT accepted until they are green. ⚑⚑ **THE THREE SPECS ARE GREEN — 3 passed in 33.1 s** on the test project once it was moved to Micro compute (`e2e/customer-journey/self-sign.spec.ts`). One of them failed first, and the failure was the product being right: a job with every room measured and confirmed reached **DETAILED, not CONFIRMED**, because the ceiling height was still an assumption. `accuracy.ts:69` docks a room 0.15 while `H` is assumed — even a confirmed one — so 0.95 credit becomes 0.80, the base caps at 80% and the whole-job checks add at most 6: **86% against a 90% bar, mathematically unreachable**. Height is therefore a hard precondition for self-serve, which nothing had written down. The spec now taps `confirm_height` (the customer's own control, `ScopeEditor.tsx:719`) and passes. ⚑ Tom, 11 Sep: the customer is told none of this — flagged into C10 (the finish line must NAME the blocker) and C9 (moving height to the details screen must reuse `confirm_height`, or one answer stops crediting every room). The dock is stored per-area but the ANSWER is already per-house: `confirm_height` maps over every interior room and guards Exterior elevations, whose H is a measured facade height. |
| C7b | Estimates page becomes the home of estimating; pack as a tab; C5 queue folded into the one evaluator | DONE | 12 Sep 2026 | _(branch `feat/c7b-estimates-home`, PR pending)_ | none, as expected | — | 2242 → 2290 | 38–42 (defaults taken) | **Step 1 answered: NO FOLD-BACK NEEDED.** C5 never built a second queue — `buildDeskCheckItems` is inside `lib/crm/work-queue.ts:604`, spread into the one item list as `kind:"desk_check"`; `app/crm/queue.ts` is the single entry point. The **Waiting on you** tab (first, default) renders `getWorkQueue()` through `estimatesPageItems()` — a filter that lives BESIDE the evaluator so the parity test (`desk-check-queue.test.ts`) can prove Today and the tab are one list; `WaitingTable` runs no query, buckets nothing, counts nothing. `WorkItem.valueCents` kept by `finish()` (Tom ruled A, 12 Sep: widen the contract, never a second query). The **pack is a tab**: `app/quote/desk-check/` → `app/quote/PackPane.tsx`, rendered by the shell on `?tab=pack` only when `builder_state.wizard.state` is an object. **Session 2 (12 Sep):** the pill vocabulary is `estimatePill()` in `lib/wizard/journey.ts` — every state DERIVED from the estimate, its latest `confirmation_requests` row, `estimate_views`, `work_orders` and the loop's own `customer.confirmed` flags; 10-row table test, no stored string. `LIST_SELECT` (`lib/estimate/listRows.ts`) reads it all in ONE round trip via embeds on existing FKs plus two JSON paths (`wizard_job:builder_state->wizard->state->>jobType` is the Pack gate — one scalar, not the tree). One action per row from the request's stored `suggested_action` (fix→Fix price, visit→Book, ask→Chase; question→Chase; viewed→Nudge; accepted+WO→Open job). Source filter is `?built=customers|inhouse` (NOT `source`, which the Wizard tab already uses for entry_source; in-house is `.or(neq,is.null)` because `<>` drops nulls). Value is a range via `rangeFromTotal` on the server. The **strip** (`app/quote/EstimateStrip.tsx`) sits above the tabs on every tab; its figures and the Pack tab render ONE `PackBundle` from `app/quote/pack-load.ts` (the loader lifted out of PackPane); its three buttons (`StripActions.tsx`) POST to the C6 route unchanged and are offered only when an open request exists. Scope edges: `provenanceEdges` prop on `QuoteBuilder` (the shell passes `hasWizard`), one class on the folder row, `data-provenance=confirmed|assumed`. ⚑ TRAPS: (1) `daysUntil` first hardcoded `+10:00` and the repo's boundary guard caught it — it now goes through `melbourneParts`, the same clock `holdUntil` writes with; (2) the list's ±band is the SUBMIT-TIME snapshot, the strip's is live — parking lot; (3) `e2e/desk-check.spec.ts` and `confirmation-pack.spec.ts` still drove `/quote/desk-check` (deleted in session 1) — re-pointed to `?tab=pack`. `Brief · book a visit` is in the vocabulary but nothing produces it until C14. Not built from the mockup: assignee/date filters, tab counts, Export, extra bulk actions (parking lot). e2e: `e2e/estimates-home.spec.ts` (3 stories: lands on Waiting; Pack → lands on Pack with strip, Scope shows amber edges, source filter hides it; in-house has no link/tab/strip/edges and bulk Clear still works) — **4/4 GREEN on C1** (3 stories + the 1440px look spec, 33 s), and `desk-check.spec.ts` ×3 + `confirmation-pack.spec.ts` ×2 green in the same session once re-pointed. Visual check done from the look spec's PNGs: list, Pack tab and Scope tab all carry the strip; the pill vocabulary rendered on real rows ("Sent · confirm remotely · 0 of 8 · ±15% · 2m ago"). PR pending; no SQL. |
| C7c | Test-project hygiene: e2e teardown, scheduled sweep, row-count tripwire, CI secret and trigger rules | DONE | 12 Sep 2026 | _(branch `feat/c7c-test-hygiene`, PR pending)_ | **none on prod**; no scheduled function either — `pg_cron`/`pg_net` are available but not installed on the test project and installing them would be a `create extension` that must never reach production, so the sweep is a GitHub Actions cron (`.github/workflows/hygiene.yml`, 03:00 Melbourne) | — | 2290 → 2310 | 43–47 (defaults taken; 46 and 47 answered below) | **Step 1: four of the five 11 Sep items were on main; the fifth (recreate) was not** — the project holds 68,512 estimates, 40,110 invoices, 29,903 accounts, 10,986 anonymous users and 257 `pg.e2e.*` logins in 610 MB; Micro compute alone restored it. ONE implementation, three triggers: `scripts/c1/hygiene.mjs` (`count` / `teardown --since` / `sweep --age-days --batch`) over the test project's Postgres string (`C1_DATABASE_URL` locally, **new repo secret `E2E_DATABASE_URL` in CI — Tom must add it; until then the e2e job fails at the secrets check, by the standing rule**). The delete walk is CATALOG-DRIVEN (`pg_constraint` read at run time, `fkAction` in `hygiene-rules.mjs`): CASCADE → descend, SET NULL → leave, RESTRICT → purge, NO ACTION → purge when owned/NOT NULL else nullify; so the six no-action columns the brief names are handled without a hand-written order that the next migration would break. Guard `checkTarget`: production NAMED, database nameable, not production, same project as the app — all four refusals unit-tested; exclusion list by id from `E2E_*_EMAIL`, non-empty or refuse. **Measured 12 Sep: 25 users with their estimate chains, leads, sources and `@example.com` accounts in 4.3 s (~0.17 s/user, not 13.5 s — the hot FKs are indexed on the test project).** Tripwire: index-only scan on `users_is_anonymous_idx` (cost 392) and an index filter for `pg.e2e.%` (cost 392); counts logged every run; warn 5,000 / fail 20,000; exit 2 stops the suite. Teardown by the marker `global-setup.ts` records from the DATABASE clock (never a pattern over all time), runs after a failing suite (Playwright's globalTeardown). ⚑46: next tier above Micro is Small, about US$15/month, then Medium about US$60 — from memory of Supabase pricing, confirm on the dashboard; on today's numbers Micro holds once the sweep has run. ⚑47: 51 columns reference `auth.users`, 35 unindexed — the list is in the parking lot. ⚑ FINDINGS: anonymous users grow with NO e2e running (+10 in 12 min) — `lib/wizard/session.ts` mints one per `/estimate` visitor on any deployment wired to the test project; only the STAFF seeded login exists on the test project (contractor/customer specs skip on C1). **PROVED 12 Sep (local, :3102 because the help worktree held :3101):** tripwire line logged at start (11,281); a deliberately failing spec + the three C7b specs; global teardown ran after the failure — "created 3, deleted 3 users, left 0, 1.6s", users before 11,284 → after 11,281 = the starting count ±0; staff signed in fine after a sweep. CI acceptance (before/after lines in the job log) waits on `E2E_DATABASE_URL`. **Backlog sweep run locally 12 Sep: 3,000 users in 441 s (0.15 s/user) — 4,034 estimates, 2,975 accounts, 2,164 leads, 474 properties with them; test project 11,285 → 8,299 run-created users, 3,125 older than 3 days left for the daily workflow at 200/day (raise `batch` via Run workflow to clear it sooner).** Rules 3 and 4 added to `CLAUDE.md` (1 and 2 were already there from 11 Sep); incident note `docs/reference/2026-09-11-test-project-outage.md`. Next: C8. |
| C8 | Screen 1, Save & book, "both", email after price | DONE | 12 Sep 2026 | _(branch `feat/c8-screen1-save-and-book`, PR pending)_ | none, as planned (`last_screen` from C3 is the resume point; no `resume_screen` column) | **none** — `email_gate_position` and `office_phone_display` deliberately NOT seeded (parking lot: ⚑1 is built, the phone is `company_profile.phone`) | 2290 → 2295 | 1, 25, 26 | **Already built before this chunk:** email after the range (`Reveal.tsx` "Keep this estimate" → `/api/wizard/keep`) — the quick-look path asks no email before the reveal, now pinned by the e2e. BUILT: screen-1 "Rather not fill anything in?" card (Book someone in → the sheet; Call us → `tel:` from Settings); the **Save & book pill** in the wizard header on every quick-look screen and the reveal; the sheet (`app/wizard/SaveAndBookSheet.tsx`: email required ⚑26, mobile optional, four offered windows from `wizardVisitSlots`, Save and book / Call me back, the phone line); **one route** `app/api/wizard/save-and-book` (GET slots, POST): the SESSION is the draft (found via `findOpenDraft`, the wizard's own lookup), marked `visit_requested` / `ready_visit` with the resume point in `last_screen` — which puts the lead on the work queue through the existing `wizard_ready` trigger, no second list; `ensureAccountAndProperty`; with an estimate: `confirmation_requests(kind=visit)`, the slot booked via `bookWizardSlot`, `confirmation_requested` + `visit_booked_from_wizard` events; magic link last and best-effort; idempotent (`isRepeat`). **"Both"** → the choice screen (`s-both`, step `both` in `stepsFor`, excluded from the screen-1 count) → two ranges on the reveal (⚑25): the submit route prices the Interior and Exterior blocks as two `editorPayload`s and `customerPayload` carries `parts`; the whole-job range stays the headline. **PagePaint stripped** from the customer path (the contact page's embed removed; the exterior extras embed is staff-only). **Staff open a session** at `/estimate?session=<draft id>` (staff-only, service read, no adoption) and land on `last_screen` under `assisted-banner`; the journey drawer links it. ⚑ NOT built, with reasons in the parking lot: "Now price the outside" after an interior reveal (the submit route has no replace path — a second submit is a second estimate); the sheet's extras list; the showcase link (no public route); write-through assisted sessions. **E2E 4/4 GREEN on C1** (`e2e/customer-journey/save-and-book.spec.ts`: nine-tap range with no email field on any screen; Save & book from screen 3 → the draft reads `visit_requested` / `ready_visit` / `last_screen = quick:job`, and staff opening `/estimate?session=` land on screen 3 under the banner; "both" → choice → two ranges; the drive still walks a both job) plus `both-stacked` and `exterior-quick-look` green. ⚑ TRAP the e2e caught: a fast walk can post Save & book BEFORE the first draft insert lands (the autosave is debounced and the flush at open is fire-and-forget), so the route found no session and the promise fell on the floor while the sheet said "saved". The route now retries once and, failing that, CREATES the session from a snapshot the sheet sends — the sheet never says more than the server did. Next: C9. |
| C8b | Exterior quick look rebuilt — elements first, materials only if the body is painted, window type and counts, colours, storeys once | DONE | 12 Sep 2026 | `f06b574` (merged to main 12 Sep at Tom's instruction, no PR) | none | **none — ⚑48 declined:** the card already prices each window type as its own rate row (Awning / Casement, Double Hung Sash, Colonial / Bay, Fixed / Picture), so a multiplier on top would double-count; the type → rate row map is `lib/wizard/starter.ts` `EXTERIOR_WINDOW_CODE` | 2376 → 2386 | 48, 49, 50, 51, 52 | **The outside screen** (`app/wizard/QuickLook.tsx`, prototype v2.6 `s-ext-job`): elements first (body, windows, doors, fascias, gutters, eaves), then the freestanding things — NOTHING pre-ticked; materials only if the body is ticked (nothing pre-ticked, "not sure" exclusive); window type + count only if windows; door count only if doors; colours (same three-way shape, ⚑51 — writes `condition.tier` on an exterior job so the exterior rule derives from it); condition; storeys ONCE (the place screen hides bedrooms and storeys for an outside job); access; the book-someone-in card. Continue refuses an untouched screen. **No bedrooms:** `quickLookToState` no longer writes `basics` on an exterior session (unit); the reveal's words come from `exteriorRestatement` / `exteriorAssumedList` (`lib/wizard/exterior-quick-look.ts`). **The seed** (`starterExteriorNodes` + `counts`): whole-job counts spread front/rear/left/right; the first door is the front door; aluminium → no line + a flag (⚑49); winder → casement rate + flag; not sure → the old Fixed / Picture placeholder + flag; body with no material → placeholder wall + `exterior_material` flag; older sessions (no counts) seed exactly as before. **What we'll do for the outside** (`lib/wizard/systems-view.ts` `exteriorWhatWeDo`): walls by material, windows by type (the colonial line says why), doors, fascias, gutters, eaves, freestanding, then Not included — never the interior lines; a both job gets both sets. ⚑ **TWO BUGS FOUND BY THE E2E, both older than C8b, both fixed here:** (1) the quick look never wrote `state.surfaces`, so every outside job since C8 carried the INTERIOR default ticks and the seed dropped every window, door and trim — walls only (`applyExteriorQuickLook` now writes `exteriorSurfaceKeys`; a both job keeps its interior ticks); (2) three window codes exist on both sides of the card and `substrateKeyForRateCode` answered INTERIOR for all of them, so an exterior casement line was judged against the interior windows tick — the lookup now takes a side (`lib/estimate/substrates.ts`; the merge, the sides editor and the exterior view pass it; `substrateOptionsFromRates` offers a substrate on its own side only). **E2E GREEN on C1:** `exterior-quick-look.spec.ts` ×4 (nothing pre-ticked / gated questions / storeys once / no bedrooms; Tom's check as three walks: casement range with exterior words and exterior What-we'll-do; **colonial lo > casement lo**; body unticked → no materials question, no Walls line, no `.sd-wall` on the sides), plus `both-stacked` ×1, `exterior-path` ×1, `parity-mechanics` ×2, `save-and-book` ×4, `outside-commercial` ×7, `human-moments` ×2 = 21 green. `sides-editor.spec.ts:46` FAILS on `human-line` — **proved pre-existing on main** by stashing C8b and re-running (parking lot: the sides-only footer never got C11's line). ⚑50 whole-job vs per-side counts: the rule is reported in the parking lot, not built. ⚑52 per-elevation allowances still absent. |
| C9 | What's changing colour, details, What we'll do | DONE | 12 Sep 2026 | _(branch `feat/c9-colour-details-what-we-do`, STACKED on `feat/c8-screen1-save-and-book` — PR against C8's branch, retarget to main after C8 merges; verify with `git log origin/main`, not the badge)_ | **none** — `deriveSystem` was already TABLE-driven: the `paint_systems` Settings row holds a rule per group × intent plus the flags and scalars (`PaintSystemsSettings.tsx` edits it) | none new (`paint_system_rules` from this row's plan IS `paint_systems`) | 2295 → 2306 | 2, 3, 4, 5 | **The audit the block asked for:** rows, not code — so the change is to the ANSWERS, not the rules. `SystemAnswers.intents` (per group) is new in `lib/pricing/systems.ts`; `deriveSystem` takes the group's own intent when named, else the job-wide one, so every pre-C9 estimate derives exactly as before (`condition.colourAnswered` is the switch, false on old rows). The job screen (`QuickLook.tsx`) replaces the colour picker with the three tiles + "lighter or bold" + "still choosing"; `colourFromChanges` derives the old `colour`/`tier` so nothing downstream moved; `groupIntents` (systems-view.ts): unticked → same, ticked or undecided → new, bold lifts ONLY a ticked group (Tom's 10 Sep "the tick earns the coat" kept); windows follow trims; the editor's ceiling answer and the tile agree. **What we'll do** is ONE read-only component (`app/wizard/WhatWeDo.tsx`) on the reveal (lines ride `CustomerPayload.systems` from the submit route), the tighten screen (the editor keeps `paintSystems` from every reprice response — it was already sent, just not rendered since Tom removed the card) and the finish line. No buttons in it, asserted by the e2e. Details: the tighten card asks **"are the doors and skirtings shiny?"** (⚑5) via the existing `set_paint_system glossTrims` action while the trims line carries `review`; `PageDetails` (describe/upload) gets the same question and loses door-only-vs-frame for customers (staff keep it — the rate card prices both). Golden tests added (S2·3 same vs new differs only in the changing groups, S2·5 allowances identical across intents, S2·6 rule rows round-trip the Settings parser) beside the existing engine-arithmetic and one-coat guards. ⚑ The plan's table still says two coats for same-colour trims; the repo's 9 Sep reversal rules (parking lot). NOT built: the photo shortcut on the details card (needs the reader to classify a photo — C15), the two extra window options, picture tiles on the page-set version. **E2E GREEN on C1:** `e2e/customer-journey/what-we-do.spec.ts` ×2 (doors and trims ticked → the trims line is the rules' new system with no primer; Shiny on the details card → the bonding-primer sentence, one more coat, undercoat, the range moved, the question gone, and not one button in the panel; nothing ticked → walls one coat, no undercoat anywhere) plus `save-and-book` ×4, `simpler-form` ×3 and `both-stacked` green with the colour block in place. ⚑ TRAP the e2e taught: the default trims/doors "new" system is ALREADY undercoat + two coats (Tom, 9 Sep), so Shiny makes it four, not three — the spec asserts "one more than before", not a number the rules own. Next: C10 (merge `fix/spot-extent-quantity` first). |
| C10 | Tighten: rooms, spots, one missed sheet, site & access | DONE | 12 Sep 2026 | _(branch `feat/c10-tighten-rooms-spots-access`, STACKED on C9 — PR against C9's branch; `fix/spot-extent-quantity` REBASED onto it as its first two commits `74d2f07` + `cc33229`, two small conflicts resolved: RoomSpots.tsx import line, ARCHITECTURE.md both sides kept)_ | none | none | 2306 → 2321 | 6 (default kept: crack/hole auto-price, the rest review) | **Already there:** the room list with assumed sizes and status, the room card's size/surfaces/counts/robe doors, spots with photos (`RoomSpots`), refresh-resume (C3). BUILT: the per-room condition chips are GONE from the customer card (v2.5; `set_room_condition` stays on the route for staff); **extras in this room** (`RoomExtras.tsx` + `lib/wizard/room-extras.ts`: feature walls counted, wallpaper to strip, something else) as review lines pinned to the room via a new `room_extra` action — no rate codes exist, so nothing is priced from them and the card says so; the two whole-job checks folded into **one "Anything we've missed?" card** (`data-card="sweep"` kept so six specs, `openAndScroll` and the finish line's Change links all still address it; both checks and both confirms survive inside it, so `checksDone` is unchanged); the condition free-text box is off the quick look (kept on the describe route); site & access loses **pets**; **`hazmat_check` written to `site_checklist_items` at every confirmation** (idempotent upsert); the reveal's assume list **deep-links** (`#rooms` / `#details` / `#access` / `#missed` / `#systems`) and the editor opens and scrolls the named card on arrival. ⚑ NOT changed, with reasons in the parking lot: the hard-stop rule (a safety rule only an explicit yes can trip — unreachable for customers, pinned by six tests), floors (Tom does not price it), the sides editor's own cards. **E2E GREEN on C1:** `e2e/customer-journey/tighten-c10.spec.ts` ×3 (three rooms confirmed → the score climbs each time and the range never widens; a crack flagged with a photo → "repair priced" on the room and a `prepHr > 0` crack line on the estimate row; one "missed" card with both checks; a feature wall → a `room_extra:feature_wall` deferral on the row; no pets; the assume-list link lands with the first room open; no free-text box on the condition screen) plus the six specs that drive the check cards, `what-we-do`, `doors-tiles-steppers`, `exterior-path`, `outside-commercial` and `wizard-buckets` green after their stale assertions were updated (the paint page no longer rides the customer contact page; "both" meets the choice screen; a converted session's pill sits on its estimate row). ⚑ The full customer-journey + RLS set ran on the stack before C10: **113 passed, 1 skipped, 3 failed**, and all three were the stack's own stale specs, not CI's environmental class — the assistant specs that are red in CI pass locally. |
| C11 | The human moments and the reveal | DONE | 12 Sep 2026 | _(SAME BRANCH AS C10, `feat/c10-tighten-rooms-spots-access`, one PR — C10 and C11 edited the same editor files in interleaved hunks, so two compiling commits were not separable)_ | none | none (`profiles.patch_postcodes`, `profiles.phone`, `company_profile.coordinatorName` already exist) | 2321 → 2330 | 13 | **ONE resolver** for who the estimator is: `lib/wizard/estimator.ts` `resolveEstimator` (patch postcode → staff profile, else the Settings coordinator, else NOBODY — `sendTo` is now its name, so the CTA, the hand-off page and every new surface agree). **The strip** (`app/wizard/EstimatorStrip.tsx`: initials, name, the customer's suburb only when the patch covers it, Book a time / Call) on the reveal (rides `CustomerPayload.estimator` from the submit route), the tighten screen (above the questions, and as the reach strip's header — the "Book in your estimator" lead and the icon tiles are GONE) and the finish line. **One footer human line** from `lib/wizard/human-line.ts` (all done → condition work → not-sures ≥ 2 → partly done → default; table test), with Book a visit beside it; the old "N of M confirmed, tap Send" counter is deleted. **Inline offers** (`app/wizard/Offer.tsx`) at the three moments: the size adjuster opening, the spot panel opening (inside `RoomSpots`), and the second not-sure on the details card. **Tier words over the one ladder**: the finish line read `bandPct >= 15` locally — it now takes `initialLadder.tier` from the bundle; the reveal's Guide is by construction. **The roller reveal** on the range with `prefers-reduced-motion` honoured (wizard.css `wzroll`). ⚑ On the C1 test project nobody is named (no patch, no coordinator) so every surface says "Your estimator" / "we" — correct, and why Tom's check needs a patch seeded (parking lot). **E2E GREEN on C1:** `e2e/customer-journey/human-moments.spec.ts` ×2 (the strip on all three screens with the SAME name, never "Sarah"; Guide lit on the reveal; no "Book in your estimator", no `.reach-b` tiles; the human line with Book a visit and no counter; the finish tier from the ladder; the line in `not_sures` on a fresh walk and moving on once the details are answered; the measure offer on Adjust it; the damage offer on Point out a spot) and `reach-and-chat` ×3 green with the counter assertion replaced. **LIGHTHOUSE (mobile, `/estimate` — the reveal's page — via `npx lighthouse@12` against a local `next start` of the C1 build): performance 88, accessibility 100, best-practices 100; LCP 3.1 s, TBT 70 ms, CLS 0.** Below the ≥ 90 bar by two points, and LCP is the whole gap — recorded as a MISS, not a pass; parked for C17 hardening (font and hero image loading on the wizard shell). |
| C12 | Segments as data, office pattern, commercial reveal | DONE | 12 Sep 2026 | `37e62ea` (merged to main 12 Sep at Tom's instruction, no PR — GitHub refuses a PR for a fully merged branch; carries `docs/c8b-handover` `905337e`) | `20270140000000_commercial_segments.sql` — **RUN on TEST and PROD 12 Sep** (Tom's pastes; test verified by `auth.users` count 8,751, prod confirmed by Tom) | `commercial_pricing` (⚑20 widen 5 / +3 no photo, ⚑21 loadings, ⚑22 racking, EWP threshold 4 m, ⚑32 charge-out null) — seeded on test and prod 12 Sep | 2330 → 2376 | 19, 20, 21, 23, 32 | **The row decides.** `commercial_segments` (8 tiles + the exterior route; config, brief, typicals with each room's scope type and a `surfKeys` map) is the source; `lib/wizard/segments.ts` `DEFAULT_SEGMENTS` mirrors the seed and `segments.test.ts` parses the SQL to prove they match — edit the seed, and the test says where the mirror drifted. `loadSegments` falls back to the mirror when the table is absent (the test project HAS NOT run the migration; every e2e below ran on the mirror). **Three screens from the row** (`app/wizard/CommercialScreens.tsx`): segment tiles tagged from `route`, inside/outside/both (outside → exterior brief, both → one visit — today the person screen with the reason; C14 builds the brief), not-sure card, trade note; areas (kind, count steppers, open-space card: size bracket / ceiling / wall height in height mode / photo — no partition or frontage question); job (row surfaces, **C9's colour tiles reused**, condition bands in the row's words, hours, occupied). `stepsFor(jobType, propertyKind)`: commercial inside = start → place → segment → com_areas → com_job. **Engine** `lib/pricing/commercial.ts`: open areas square at the bracket midpoint (35/100/275/550 m²), walls = full perimeter × height, ceiling surface removed + flagged unless plaster, wall height 3.6/5/7.5 m in height mode, ONE flagged `commercial_ewp` line above the threshold; `Adjustments.hourLoading` multiplies derived production hours only (never prep/allowances/materials; a typed override is final). Four goldens green, residential goldens unchanged. **Door** (`lib/wizard/commercial.ts` `routeCommercial(key, {segments, kind, jobType})`): range → `commercial_range` (reveal, `walkthroughRequired` — **never fix-online**, unit + e2e), brief/hospital/outside/both → `commercial_brief` handoff; the seven gates are retired (stored gate answers ignored; `commercial_gate_*` WHY lines kept for old drafts). `answersFromState(state, segments)` resolves the door once from the rows. **Reveal**: kicker `<Segment> · your guide range`, `commercialRestatement`, `commercialAssumedList`, the commercial note (`data-widen`), band = accuracy band + `commercialWidenFor` (submit, tighten data and edit route all apply it). **E2E GREEN on C1 (mirror, no table):** `commercial-segments` ×4 (tiles from the table when present else 8 mirror tiles with route tags; Tom's check office 4/1/1 → range in two screens, kicker Office, widen 8, restatement, assume list, no Fix online / Accept estimate on reveal or tighten; health aged → online, hospital → person; school hall 9 m → EWP on the settle list), `outside-commercial` ×5 rewritten (hand-off moved to the segment screen), `human-moments` ×2, `save-and-book` ×4 = 17/17. `commercial-kind.spec.ts` (gates) deleted. `grep -i glass` over the diff: empty. ⚑ Commercial via the describe/upload routes still seeds HOME rooms (parking lot). ⚑ The EWP line is unpriced until a hire figure exists. |
| C13 | The warehouse pattern | DONE | 12 Sep 2026 | `25c06c4` (merged to main 12 Sep at Tom's instruction, no PR) | none | none — ⚑22 racking, the EWP threshold and the `operating` loading were already in `commercial_pricing` (C12) | 2386 → 2402 | 22 | **The warehouse screen** (`app/wizard/CommercialScreens.tsx` `WarehouseScreen`, step `com_warehouse` replacing `com_areas` when the row's `pattern` is `warehouse` — `stepsFor(jobType, kind, pattern)`): floor-area brackets to over 5,000 m² or a typed L × W (typed beats the bracket), height to the underside of the roof with the scissor-lift note, the ten industrial surfaces with counted roller doors / personnel doors / offices (a tile is a `div[role=button]` — the counted rows carry real stepper buttons), wall material only if the walls are ticked and NOTHING pre-ticked (the C8b ruling; the prototype's precast pre-tick dropped), racking / operating / lift on site. Continue refuses an untouched screen. The shared job screen is unchanged (the row's `surf: null` renders no surface tiles, its wear/work copy and hours; no occupied question). **The tree** (`lib/wizard/warehouse.ts`): ONE interior room "Warehouse floor" (L × W × H from the answers, so walls = 2(L+W) × H through `computeQuantity`) whose wall lines are the card's cladding rows per material (precast → Concrete / Tilt Slab, blockwork → Brick (Unpainted), sheeting → Colorbond Cladding, cement sheet, plasterboard → Walls; not told → the tilt-slab placeholder flagged), split evenly when several, each × the racking share as `sharePct` (100 / 88 / 70 from Settings); roller doors = count × Garage Door (1 Car) ONE face (an inside job never ticks an outside element); personnel doors = count × 2 × Standard Door (1 Side); offices and amenities from the office row's typicals ("priced like an office"); roof, steel, bollards, line marking, mezzanine, the material's prep (sealer / block filler / metal primer as a crew note) and the racking = flagged, unpriced `deferred` lines that the tighten screen shows as ⚑ confirmed before your final quote. Operating = the `operating` loading through `hourLoadingFor`. **ONE engine change** (`lib/pricing/estimate.ts` `priceSurface`): a code with no row on the area's own side prices at the other side's row — the industrial rows exist only on the Exterior side of the card; the charge-out stays the area's own. `DraftSurface.sharePct` added. **Goldens green** (`lib/wizard/warehouse.test.ts` ×16): racking=most = exactly `racking.most` of racking=no on the wall m²; 4–6 m without a lift = one EWP line, with a lift none, up to 4 m none, the threshold from Settings; roller doors = count × the per-face row (3 doors = 3× one); the flags; the merge keeps the floor's lines; no beds or storeys key on a warehouse session (schema-parsed); the reveal's words. **E2E GREEN on C1:** `commercial-warehouse.spec.ts` ×3 (the screen: defaults, no bedroom/storey text, materials only if walls, steppers, nothing-ticked refused; Tom's check 1,000–2,500 / 4–6 m / precast / some racking / operating → range with "large warehouse, 4–6 m high, racking against some walls, operating during the works", the scissor-lift line on the assume list and the settle list, the precast prep and racking flags, Warehouse floor + Office 1 on the tighten screen, no Fix online / Accept estimate; a lift on site removes the line) + `commercial-segments` ×4, `exterior-quick-look` ×4, `save-and-book` ×4 = 15/15. ⚑ No What-we'll-do line for the floor's walls or doors yet (exterior substrate keys have no interior card group — parking lot). ⚑ Industrial prep is a note and a flag, never hours — the derivation table has no industrial rows (parking lot). |
| C13 | Warehouse pattern | TODO | | | none | 22 | | 22 | |
| C14 | Briefs, booking, commercial exteriors | TODO | | | `commercial_briefs`, `site_checklist_items` | 27 | | 24, 27, 29, 30 | replaces C4 stop-gap |
| GATE | Trade-portal prototype approved | TODO | | | — | — | | 36, 37 | design deliverable, not a session |
| C15 | Building profiles, measured trees, saved specs, spec sheet | TODO | | | `building_profiles`, `properties.measured_tree`, `trade_specs`, `tenant_photo_links` | — | | 11, 12, 34, 35 | |
| C16 | Assistant hooks | TODO | | | none | — | | 14 | |
| C17 | Hardening and the switch | TODO | | | none | — | | A, B, F | CI e2e ≥ every customer-journey spec |

## Rulings recorded (so they are never re-asked)

| Date | Ruling |
|---|---|
| 10 Sep | Glass share not asked; walls at full perimeter, cutting-in offsets |
| 10 Sep | Retail includes hospitality and restaurants |
| 10 Sep | Healthcare asks aged care / clinic / hospital; hospital → brief; counts "resident rooms, wards or treatment rooms" |
| 10 Sep | Every school exterior is a visit; every commercial outside or both is a visit |
| 10 Sep | Every ranged segment tile says "online · or we visit" |
| 10 Sep | No "how we'll paint each surface" screen; "what's changing colour?" tiles; details screen with pictures; What we'll do read-only |
| 10 Sep | The estimator is named and present; one footer human line; inline offers; "Send to <name>" |
| 10 Sep | Removed: per-room condition, asbestos, pets, the hard stop, hospital beds and infection-control rows; hazmat check on the site checklist |
| 11 Sep | Phase 1 is the estimator loop; nobody self-signs before C1 and C2 |
| 11 Sep | No separate lead queue — confirmations are a trigger in `lib/crm/work-queue.ts`, surfaced as a tab on the estimates page |
| 11 Sep | The pack is a tab on the estimate, present only when there is wizard data; Scope stays the existing editor |
| 11 Sep | **Incident:** test project `qarfyjrzgdeoqbnbbxfp` exhausted its IO budget — seven concurrent e2e runs on a t4g.nano over months of uncleaned test data. Production untouched and proved so (33 estimates, 0 auth errors, no `pg.e2e.*` rows). Project recreated from `docs/testing/c1-test-project.md` |
| 11 Sep | A missing environment variable stops the run. No guard may hardcode or infer a project ref, and an unidentifiable target is never treated as safe |
| 11 Sep | No CI secret may fall back to another environment's secret. A missing secret fails the job. The three `E2E_*` secrets exist in their own right; the plain-named ones were deleted |
| 11 Sep | e2e runs one at a time (`group: e2e-test-project`, `cancel-in-progress: false`) and only on pull_request, main, or `workflow_dispatch` |
| 11 Sep | Exterior: elements first (body, windows, doors, fascias, gutters, eaves); materials asked only if the body is painted, nothing pre-ticked; window type and counts; door count; colours; storeys once; no bedrooms ever |
