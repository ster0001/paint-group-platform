# Estimator v2 — parking lot

Things noticed outside the chunk being built. One line each, with a `file:line`. Nothing here is
acted on in the session that found it (run sheet §1: no "while I was in there"). Tom decides what
graduates into a chunk or its own PR.

| Found in | Line |
|---|---|
| C0 | **RESOLVED** — `fix/seed-target-guard`. Five (not six) seed scripts resolved their target from `.env.local` and ignored the environment: `scripts/portal/seed-demo-customer.mjs:1`, `scripts/seed-demo-loop.ts:1`, `scripts/create-test-customer.ts:1`, `scripts/create-test-contractors.ts:1`, `scripts/seed-extraction-settings.ts:1`. `scripts/c1/seed.mjs` was already guarded by `scripts/c1/env.mjs` `refuseProduction` — the C0 report over-counted it. |
| C0 | Three files cited by `docs/briefs/claude-code-brief-estimator-journey-v2.md:§1` do not exist in any ref: `docs/briefs/wizard-project-allowances-spec.md`, `docs/briefs/rebuild-plan-v2.md`, `docs/briefs/business-inputs.md`. None are in the run sheet's Step 0 list. C10 (site & access) is the first chunk that wants the allowances spec — `lib/wizard/site-access.ts:135` was already built without it. |
| C0 | Migration number collision on disk: the main checkout holds untracked `supabase/migrations/20270111000000_contractor_invoice_submit_remainder.sql` while main carries `20270111000000_room_allowances.sql` at the same number. |
| C0 | Seven duplicate migration numbers on main (`20260917`, `20261211`–`20261214`, `20261229`, `20270110`) and a five-number gap (`20270115`–`20270119`) that exists on no branch. Filename, not number, is the identity — `supabase/migrations/`. |
| C0 | `20270112` (`fix/qa-recheck`) and `20270113` (`fix/console-tick-index`) are described in session notes as "queued for prod" but are on unmerged branches, not on main. Either merge them or stop counting them. |

| C0 | Two production-refusal mechanisms now exist: `scripts/seed-target.mjs:34` hardcodes `PRODUCTION_REF`, while `scripts/c1/env.mjs` `productionRef()` derives it from `.env.local`. Neither is wrong and they guard different scripts, but they should be one helper. Not unified here — outside the ported change. |
| C1 | `wizard_public.enabled` is FALSE on the C1 test project, so `/estimate` serves the holding page and EVERY `e2e/customer-journey/*` spec fails at `openQuickLook` (`e2e/customer-journey/drive.ts:135`). `e2e/global-setup.ts` does not set it. Almost certainly one of the CI-red classes C17 has to triage — the fix is probably one line in global-setup. |
| C1 | The reward promises the branch carried (30-day hold, first pick of start dates, paint upgrade, Dulux consult) are deleted, not relocated. Plan §2.2 says the true ones should be stated as facts at the moment they apply — that is C7 (the hold, and ruling J makes it 60 days via `hold_days`) and C11 (the human moments). |
| C1 | Two production-refusal helpers still: `scripts/seed-target.mjs:34` hardcodes the ref, `scripts/c1/env.mjs` `productionRef()` derives it from `.env.local`. Should be one. |
| C3 | A1b, the "26-second silent wait", does not resolve as a finding anywhere in the repo — only briefs citing it. The phenomenon is addressed: not silent (`app/wizard/WizardApp.tsx` processing screen), bounded (`app/api/wizard/submit/route.ts` `maxDuration = 60`), and a dropped connection is caught (`WizardApp.tsx` `runSubmit`). RESIDUAL: no client-side `AbortController` on the submit fetch — the bound is the platform's, not ours. If Vercel's limit ever changes, the customer waits as long as it says. |
| C3 | The localStorage cache is still written eagerly (400ms) rather than only on server confirmation, so it can briefly hold state the server has not seen. `pickResume` handles it by version, but a copy written between a keystroke and the first confirmation has NO version and falls back to the pre-C3 timestamp rule — `lib/wizard/resume.ts` `pickResume`. |
| C2/C4 | **RETRACTED — the premise was wrong.** I read "0 of 6,668 estimates reached the 90% accuracy bar" as evidence that real-world accuracy cannot clear it, and flagged that C7's fix-online door might open for almost nobody. Tom, 11 Sep: **the wizard has never been live** — all 6,668 are test estimates. The distribution reflects how the drivers walk the wizard (no-plan path, honesty-capped at 65%, no floorplan, no finished confirm loop), not how customers will. Nothing can be inferred about the bar from this data. C7 proceeds as written. |
| C2/C4 | **⚑ FOR C17's SWITCH CHECKLIST.** The proving window has not started. Its gate is "median staff correction under $150 across a real run of jobs" and the baseline is 6,668 rows of DRIVER OUTPUT — it measures how well the engine predicts what a test script typed. The pre-switch rows must be excluded or cleared when `wizard_public` flips, or the first fifty real jobs are drowned by 6,668 synthetic ones. `app/(app)/proving/page.tsx`, `lib/wizard/proving.ts`. |
| C2/C4 | **CORRECTED — both numbers I quoted were wrong.** "6,668 wizard snapshots" was my own query bug (`builder_state->'wizard'->'snapshot' is not null` matches a jsonb null and an empty scaffold; the check is `jsonb_typeof(...) = 'object'`). Production actually holds **33 estimates and 29 drafts** — the ~10 Tom describes, plus staff testing. The large numbers were the C1 TEST project throughout. Nothing to clean up. |
| CI fix | **⚑ AN EXCLUDED EXTERIOR SIDE NOW LEAVES NO TRACE.** A side left unticked on the exterior page set used to arrive in the editor as a card pilled NOT PAINTING, and in `builder_state` as a block marked `isOption` — an exclusion the customer could SEE on the quote. Neither exists now: the side simply does not render and no block is written. The per-side "No — skip this side" button is presumably where that decision moved, but it only exists for sides that rendered. So "we are not painting the back" is invisible and unrecorded. Found rewriting `tom-batch-7sep.spec.ts:58`; the spec now asserts the new behaviour. `lib/wizard/sides.ts`, `app/estimate/scope/SidesEditor.tsx`. |
| e2e-guard | **CORRECTED.** I reported 552 `@example.com` rows in PRODUCTION `wizard_drafts` and used it as evidence the tripwire hole had been exploited. Wrong: production has ZERO — the 552 are in the C1 TEST project, which is exactly where e2e drafts belong. The hole was real (proved from code, and layer 2 refused a live production-backed server) but was never exploited. Nothing to delete. |
| e2e-guard | The C1 TEST project currently has `wizard_public.enabled = true`. Left as found rather than toggled again — every `e2e/customer-journey/*` spec fails on the holding page when it is off, so ON is arguably the right permanent state for a test project, but it should be set deliberately (ideally by `e2e/global-setup.ts`) rather than by whichever spec ran last. `settings.wizard_public` has no updated-at trigger, so its timestamp cannot be used to tell who changed it. |
| CI | `e2e/customer-journey/tom-batch-7sep.spec.ts:247` passes alone and fails in a full run. It is order-dependent by construction: it uses `describedEstimateId` from an earlier test in the file, and falls back to HUNTING the database for "the newest estimate still waiting on its photos" — which in a full run can be one another spec made. Needs a rewrite that creates its own fixture, not a patch. The last of the four CI e2e failures. |
| C6 | **CORRECTION to an earlier claim.** I said fixing the `listUsers` paging fixed `ledger-parity:147` and `wo-rls:74`. It did not FIX them, it let them get FURTHER: they used to die in `beforeAll` before reaching teardown, and now they fail in `cleanupLoopFixture` instead — one on `invoices_estimate_id_fkey`, one on a statement timeout. Both are teardown faults surfaced by the test project's growth, not assertions, and not C6's. `e2e/fixtures/woLoop.ts` now reports WHICH upstream delete left rows behind instead of blaming the estimate. Making them pass is C17's CI work — the deletes are unchecked and the project is big enough that some now time out. |
| C6 | **⚑ NOT A TEST BUG — deleting ONE invoice times out on the C1 test project.** `db.from("invoices").delete().eq("id", <one id>)` returns `canceling statement due to statement timeout`. That is the root of both remaining CI e2e failures: the fixture's invoices delete runs UNCHECKED, times out, leaves rows, and the estimate delete then fails on `invoices_estimate_id_fkey`. CLAUDE.md's own rule is "every FK and every token/status column used in a WHERE has an index, created in the same migration" — something referencing `invoices` looks to be missing one, and a single-row delete scanning a large table is what that looks like. It will reach PRODUCTION as the data grows. To find it: `select conrelid::regclass as child, conname from pg_constraint c where confrelid = 'public.invoices'::regclass and contype='f' and not exists (select 1 from pg_index i where i.indrelid = c.conrelid and (i.indkey::smallint[])[0] = c.conkey[1]);` |
| C7 | **A real person's name is a code default that reaches customers.** `app/quote/company.ts:73` and `lib/portal/data.ts:60` both default `coordinatorName` to the literal "Felipe Martinez", and `getCompanyContact()` applies it — so a tenant who has never set a coordinator gets that name on the customer hand-off screen ("Thanks — Felipe has it", `app/estimate/sent/page.tsx:107`). C7's own chain is clean: `lib/wizard/customer-scope.ts` resolves `sendTo` from `profiles.patch_postcodes` then the Settings row with NO invented fallback, and `sendToLabel` keeps the generic button when there is nobody to name. This one is older and wider than C7 — it also feeds the quote and portal surfaces — so changing the default is its own job. Found by C7's "no hard-coded estimator name" grep. |
| C7b | **The estimates list has no pagination.** The brief's "keep pagination exactly as-is" assumed one; the page selects every row newest-first and PostgREST's 1,000-row cap is the only limit (`app/(app)/estimates/page.tsx:151`). C7b added a second read of `builder_state` for the wizard rows that can show "9 of 9", capped at 150 (`page.tsx:176`, `LOOP_READ_CAP`) — a row past the cap shows its line without the count. Pagination is the real fix for both. |
| C7b | `Question unanswered · {n}d` counts from `requested_at`, because `confirmation_requests` has no `question_asked_at` — C6's route writes only the status (`lib/wizard/journey.ts:297`). A migration adding the column and a one-line write in `app/api/confirmations/[id]/route.ts` would make the day count exact; today it is a floor. |
| C7b | The list's `±N%` and its range use the **submit-time** accuracy from `builder_state.wizard.snapshot` (`lib/estimate/listRows.ts:93`) — the list does not price, so it cannot score the confirm loop live. The strip and the pack DO (one read, `app/quote/pack-load.ts`), so a row that was tightened after submit reads wider in the list than on its own estimate. Either the wizard-edit route mirrors the live `accuracyPct` somewhere cheap, or the list accepts the lag and says so. |
| C7b | `Brief · book a visit` exists in the vocabulary (`lib/wizard/journey.ts`, state `brief`) but nothing produces it: `lib/estimate/listRows.ts:109` passes `brief: false`. C14 switches it on from the commercial brief. |
| C7b | Two ways to act on one request on one screen: the strip's buttons POST to the C6 route (`app/quote/StripActions.tsx`), while C5's "What now?" card at the foot of the pack still LINKS to the builder, the thread and the schedule (`app/quote/PackPane.tsx:222`, `PackOutcomes.tsx`). The e2e pins the card's recommendation (`e2e/desk-check.spec.ts` `outcome-*`). Fold the card into the strip or drop it — one place to act. |
| C7b | Mockup items deliberately NOT built (the brief wins on behaviour): the assignee and date filters, the counts in the tab labels (a second count), the `guide` / `trade` sub-labels under Value, Export, and the Assign-to / Mark-as-sent bulk actions — `design/reference/estimates-home-mockup.html`. |
| C7b | `Abandoned · room n of m` is derived as a `priced_no_request` session with an unfinished confirm loop (`lib/wizard/journey.ts` `estimatePill`): `bucketFor` counts a converted draft as completed, so `dropped` never fires for a row that has an estimate. If ⚑42's chase trigger lands in the evaluator, it should key on the same rule or the two will name different rows. |
| C7b | The e2e no-plan drive (`e2e/customer-journey/drive.ts` `driveNoPlanWizard`) produces an estimate with NO `wizard_drafts` row pointing at it — the C1 test project holds 68k estimates and 33 converted drafts. So a driven estimate's Wizard-status is "—" and the `priced_no_request` / `Abandoned` states can never be exercised end-to-end. Either the drive path skips the draft autosave, or `converted_at`/`estimate_id` are not being linked at submit for that path — worth a look before C17 leans on those states. |
| C7b | **CI run #371 on main (`048a4dd`, the C7 merge) is RED** — read from the Actions page 12 Sep. `typecheck · lint · unit` green (2246). `e2e (test project)` failed in "Customer journey · role RLS · ledger parity": almost every `e2e/customer-journey/*` spec fails at a uniform ~20.5 s (assistant, assistant-trade, assistant-widget, document-model, doors-tiles-steppers, draft-versioning, exterior-path, exterior-quick-look, fix-price, funnel-dropout, holding-and-honest-defaults, outside-commercial, parity-mechanics, pending-indicator, r5-editor, reach-and-chat…). A uniform timeout at the shared entry is the holding-page / anon-auth class already assigned to C17 (`wizard_public.enabled` on the test project, `e2e/global-setup.ts`), not a C7 regression — C7's own three specs were green locally on the same commit. Verify by reading one trace from the `playwright-traces` artifact before touching anything. |
| C0 | **`_prod_migrations` on the project Tom queried 12 Sep — the 14 post-6-Sep files with no row**, with the first object each creates, so presence can be checked without the ledger: `20270120` `account_contacts` · `20270121` crm lifecycle events · `20270122` account facts · `20270123` record writes · `20270124` crm messages · `20270125` status model · `20270126` campaigns v2 · `20270127` `visits`, `staff_availability`, `staff_gcal_connections` · `20270128` cement sheet rate row · `20270129` batch 7 Sep · `20270130` agent tone KB · `20270131` staff gcal scopes · `20270133` occupancy modifiers · `20270134` `staff_notifications`. Plus `20270137` (`confirmation_requests`, `site_checklist_items`) and `20270139` (two invoice FK indexes), both self-registering, both absent. The 11 Sep incident (pastes landing on the TEST project) is the first thing to rule out. `supabase/migrations/`. |
| C7c | **⚑47 — the auth.users fan-out, measured on the test project 12 Sep (production has the same shape): 51 referencing columns, 35 UNINDEXED.** Indexed (6 from `20270104` + the auth schema's own + `profiles.id`, `invoices.created_by`, `site_content.updated_by`): the hot ones. Unindexed SET NULL columns (a user delete scans each): `booking_offers.created_by`, `company_documents.uploaded_by`, `contractor_documents.verified_by`, `contractor_events.actor`, `contractor_events.acknowledged_by`, `contractor_expenses.decided_by`, `contractor_invites.accepted_by`, `contractor_invites.created_by`, `contractor_invoices.approved_by`, `contractor_unavailability.created_by`, `cost_intake.confirmed_by`, `credit_notes.created_by`, `expense_preapprovals.decided_by`, `invoice_events.actor`, `job_costs.approved_by`, `job_costs.reimburse_to`, `job_costs.recorded_by`, `presentations.created_by`, `warranty_issues.handled_by`, `wo_booking_notes.author`, `wo_checklist_items.handled_by`, `wo_checklist_items.done_by`, `wo_qa_checks.checked_by`, `wo_qa_items.done_by`, `wo_signoff.client_unavailable_by`, `wo_signoff.extension_approved_by`, `wo_updates.approved_by`, `wo_variations.released_by`, `wo_variations.raised_by`, `wo_variations.deduction_set_by`, `wo_walkthroughs.booked_by`; unindexed NO ACTION: `defect_observations.confirmed_by`, `estimate_sources.created_by`, `extraction_runs.created_by`; plus `auth.oauth_authorizations.user_id`. Not this chunk — one migration of partial indexes (`where col is not null`, CONCURRENTLY, `@no-transaction`, the `20270104` pattern) for a later batch. On the test project today a user delete costs ~0.17 s because the tables behind the unindexed columns are small; production will not stay that way. |
| C7c | **Anonymous users grow on the test project with NO e2e running** — +10 in the 12 minutes between two counts on 12 Sep, and 10,986 in total against ~40 customer-journey specs. `lib/wizard/session.ts` signs in anonymously when `/estimate` loads and `app/wizard/ChatWidget.tsx` when a chat is opened, so any deployment wired to the test project (a Vercel preview, a peer session's dev server) mints one per visitor. The sweep handles the volume; the SOURCE is worth naming — check which Vercel environments carry the test project's `NEXT_PUBLIC_SUPABASE_URL`. |
| C7c | Only ONE of the three seeded logins exists on the test project: `pg.sam.staff@gmail.com`. `E2E_CONTRACTOR_EMAIL` / `E2E_CUSTOMER_EMAIL` are in `.env.test.local` but have no `auth.users` row there, so every contractor- and customer-login spec skips on C1 and the exclusion list protects one id. `node scripts/c1/seed.mjs` creates them (`ensureUser`) — Tom's call whether to run it; the hygiene runner logs the two as "not present" every run until then. |
| C7c | CI needs one new repository secret, `E2E_DATABASE_URL` — the TEST project's Session-pooler connection string (Dashboard → Connect). Until it is added the e2e job fails at "Check the e2e secrets are configured", by the standing rule (a missing secret fails the job). The hygiene workflow refuses without it too. |
| C7c | `e2e/global-setup.ts` and `global-teardown.ts` spawn `scripts/c1/hygiene.mjs` as a child process rather than importing it: Playwright's config is loaded by its own TS transform, and the runner is plain Node with `pg`. The marker crosses via `process.env.E2E_RUN_STARTED_AT` (same process) with a file in `os.tmpdir()` as the fallback. |
| C8 | **"Now price the outside" after an interior reveal is NOT built.** The block asks for it; the submit route (`app/api/wizard/submit/route.ts`) has no replace path — a second submit creates a second estimate, so an Inside customer adding Outside afterwards would leave two drafts on the list. "Both" chosen up front does show two ranges (⚑25, `reveal-parts`). The door needs a "reprice this estimate" input on the submit route (or the wizard-edit route learning to add sides) — C10/C15 territory. |
| C8 | Before the reveal there is no estimate, so Save & book with a slot RECORDS the slot on the draft (`outcome_note: "Booked: …"`) and puts the lead on the work queue via the existing `wizard_ready` trigger — it does not book a diary window (`bookWindow` needs an estimate id). From the reveal onward it books for real. The card wording on Today says "Booked:" either way; a person confirms. `app/api/wizard/save-and-book/route.ts`. |
| C8 | Not built from the prototype: the "Anything we haven't listed" extras list at the foot of `sheet-save` (feature wall, strip wallpaper, mould…) — those are scope lines, not a booking; and "See real jobs and their prices" on `s-start` — there is no public showcase route (`showcase_jobs` renders only under `app/(app)/settings/showcase`). |
| C8 | The ledger row expected Settings keys `email_gate_position` and `office_phone_display`. Neither was seeded: the email gate is after the range by ruling ⚑1 (a position switch would reopen the ruling), and the office phone already lives in `company_profile.phone` (+ `phoneHours`), which screen 1's Call card and the sheet read. If Tom wants either as a switch, it is a Settings row and one read. |
| C8 | A staff member opening a session (`/estimate?session=<draft id>`, `app/estimate/page.tsx`) READS the customer's draft and lands on `last_screen` under the assisted banner; their own edits autosave to the STAFF user's draft (the draft route keys on the caller), never to the customer's row. Write-through assisted sessions (staff answering on the customer's behalf into the same row) are the addendum's fuller intent and are not built. |
| C8 | **The customer wizard has no help file.** `docs/help/` carries crm, scheduling, self-invoicing, work-orders — nothing for the online estimate, for any role, and C1–C8 all shipped without one (CLAUDE.md's definition-of-done says a feature is not done until it exists). Owed: `docs/help/online-estimate/customer.md` and `staff.md` (Save & book, the session open, the pack), written from the real screens after C8's e2e. |
| C9 | The plan's decisions table (`docs/briefs/estimator-journey-v2-plan.md:317`) still reads "Trims on same-colour: two; one only at `good`" while the repo carries Tom's 9 Sep reversal (same-colour trims = ONE coat, `trimsGoodConditionCoats`). The repo rules; the table is stale — worth a one-line edit so the next reader is not sent the wrong way. |
| C9 | The prototype's window row (`s-systems`) has six options (adds "Opens outwards", "Slides up and down", "Aluminium — not painted"); the block and the schema have four (casement, sash, colonial, winder). Built four. "Not painted" would be a scope removal, not a style — its own decision. |
| C9 | The details card's photo shortcut ("one photo of a door and skirting answers three of these") is NOT built: the editor's photo upload is the condition-photo control on the room cards, and pinning a photo to the door/window/gloss answers would need the reader to classify it — C15's territory (photo → answers). The three questions stay one tap each. |
| C9 | `PageDetails` (the describe/upload page set) still asks door style and window type without pictures, and its shiny question is text chips; the prototype's picture tiles are on the tighten card only for height/door/window style via `styleChip`. The page set is the minority route; parity is cosmetic and left. |
| C9 | `systems-view.ts` still exports the correction chips (`wallsChips`, `ceilingChips`, `glossChips`) that phase 4's card used and Tom removed on 10 Sep. Nothing renders them (grep: no consumer of `.chips`/`flagChips` in app/). Dead code, kept because `applySystemPatch` shares the types; a later tidy. |
| C10 | **The hard stop stays in `lib/wizard/policy.ts` as a SAFETY rule that only an explicit "yes" to asbestos, or pre-1970 + deteriorated, can trip — neither is asked on the customer path (both default to unsure, `holding-and-honest-defaults.spec.ts`), so no customer can reach it.** v2.5's "no hard stop" is delivered by the path, not by deleting the rule: six policy tests and the agent's question graph pin it for staff and the describe route. `hazmat_check` now lands on `site_checklist_items` at every confirmation (`app/api/estimates/[id]/wizard-edit/route.ts`), but NOTHING READS that table yet — the pack (C5) does not list checklist items. C14 (briefs, compliance flags "above the tree") is where the reader belongs. |
| C10 | Floors is in the `SiteAccess` type and the route's allow-list, and the prototype's `s-access` asks it — but Tom does not price it (`lib/wizard/site-access.ts:33`, 9 Sep) and the card does not ask it. Left out rather than re-litigated; a note-only question would cost nothing if he wants the painter to know. |
| C10 | Feature walls and wallpaper stripping have NO rate card codes (checked 12 Sep: no `rate_items` match feature / wallpaper / strip), so the room extras are review lines pinned to the room (`lib/wizard/room-extras.ts`, kind `room_extra:*`), never priced. Seeding two codes would let the same action price them — the prototype's "priced as its own colour" wants that. |
| C10 | "The range narrows three times" cannot be asserted literally: the band moves at the accuracy evaluator's thresholds (`rangeBandPct`), not on every confirm. The spec proves the score climbs on each of three confirms and the range never widens. The exterior sides editor keeps its own condition + extras cards; only the interior's two checks were folded into the one "anything we've missed" card. |
| C10 | `condition-brief.spec.ts` deleted with the quick-look box (v2.5); the `ConditionBox` component and its photo ask survive on the describe route (`WizardApp.tsx` page set), where ⚑14's 9 Sep ruling still stands. |
| C11 | **On the C1 test project no staff profile has a patch (`patch_postcodes` = `{}` on all 43) and `company_profile` names no coordinator**, so every customer surface reads "Your estimator" — the strip, the human line ("we"), the offers. Correct by rule (never invent a person), but Tom's phone check ("the estimator's real name appears") needs `scripts/c1/seed.mjs` to give the staff login a patch, or Settings → Company details a coordinator. Production has whatever Tom set. |
| C11 | The prototype's strip lists three suburbs ("Northcote, Thornbury and Preston"); the staff record holds POSTCODES, and the repo has no postcode→suburb table. The strip says the customer's own suburb when the estimator's patch covers it (C7's `estimatorLine` rule) and nothing otherwise. A `patch_suburbs` label on the profile would be one field. |
| C11 | Lighthouse is not in the repo; the score below was taken with `npx lighthouse` against the C1 server using Playwright's Chromium. If it is to be a gate, it belongs in CI as a step with a stored budget (`lighthouse-ci`), not a one-off. |
| C11 | `ReachStrip.tsx` kept the visit / call / call-back buttons and the slot form under the new strip; only the "Book in your estimator" lead and the icon tiles (`reach-b`) went. `Offer` ("measure") copy says "comes with them" rather than naming a pronoun — the record carries no pronoun and the rule is never to guess one. |
| C11 | **Lighthouse mobile on `/estimate` scores 88 performance (LCP 3.1 s, TBT 70 ms, CLS 0), two under the C11 bar.** Accessibility and best-practices are 100. The lever is LCP on the wizard shell — the fonts and the first paint of the quick look; measure with `npx lighthouse@12 http://localhost:3102/estimate --preset=perf --form-factor=mobile` against a `next start`. C17 hardening, or a small follow-up before the switch. |
| C12 | A commercial job that arrives through the DESCRIBE or UPLOAD routes (the property page's segment picker, `app/wizard/WizardApp.tsx` `PageProperty`) has no `state.commercial`, so it seeds rooms from the HOME basics (`app/api/wizard/submit/route.ts` no-plan branch) and reveals as `commercial_range` with a 3-bedroom layout. C12 built the quick-look path only; the page-set path needs the areas screen or a hand-off. |
| C12 | The EWP line is a flagged, UNPRICED deferral (`lib/pricing/commercial.ts` `applyOpenSpace`, kind `commercial_ewp`): the addendum's "pass-through line" has no hire figure in Settings (`commercial_pricing` carries the threshold only). A `ewpPassThroughCents` key and a priced line are one Settings edit and one branch away once Tom sets a number. |
| C12 | `commercial_segments.typicals` duplicates the `room_type_defaults` mechanism (`lib/wizard/starter.ts:31` `typicalSize`) for commercial room sizes; `business-inputs.md` (⚑23) does not exist in the repo, so the seed carries the prototype's sizes. Tom edits them in the table; nothing shows them in Settings yet. |
| C12 | Phase 7a's seven gates are retired as a wall but their remains stay for stored drafts: `commercialGates` on the customer schema (`lib/wizard/state.ts`), the `commercial_gate_*` WHY lines in `lib/wizard/policy.ts`, and `GuardrailAnswers.commercialGates`. Deletable once no draft older than 12 Sep 2026 can resume. |
| C12 | The builder shows LOADED production hours on a commercial estimate with nothing saying so: `builder_state.hourLoading` and `builder_state.commercial` are written by submit (`app/api/wizard/submit/route.ts`) and read by `adjustmentsFrom` (`lib/pricing/context.ts`), but `QuoteBuilder.tsx` has no indicator and no control for the loading (⚑21 is a Settings row only). |
| C12 | `loadSegments` is called per request in `lib/wizard/customer-scope.ts`, `app/api/estimates/[id]/wizard-edit/route.ts` and the submit route with no process cache, unlike `loadScopeRules` (`lib/extract/scope-cache.ts`). One small ordered select; a cache with the same TTL pattern when it shows in traces. |
| C12 | `commercial_rates` (sector bands, `supabase/migrations/20260813010000_ratecard_v7_schema.sql:52`, 7 rows on the test project) is not consulted anywhere on the range path; §4.20's cross-check lives in `fix_price`, which commercial never reaches by construction (§4.15). The band check belongs on `send_for_confirmation` for commercial — C14 or later. |
| C8b | The page-set exterior route (describe / upload, `app/wizard/WizardApp.tsx` `PageExteriorHouse`) still starts from `defaultExterior()` (`lib/wizard/state.ts` `substrates: ["weatherboards"]`, `painting.body: true`) — v1's pre-tick and materials-first order survive there. C8b rebuilt the quick-look path only. |
| C8b | A BOTH job still asks storeys twice: on the place screen (the interior needs it, `app/wizard/QuickLook.tsx` place block) and on the outside screen. The exterior-only path asks once; the both path needs one answer written to both `basics.storeys` and `exterior.storeys`. |
| C8b | ⚑50 whole-job window/door counts vs per-side counts: the seed spreads the totals front/rear/left/right in turn (`lib/wizard/starter.ts` `spreadOverSides`) and the sides editor counts per side (`lib/wizard/sides.ts` `dwTotals`); no reconciliation rule exists when the totals disagree after the customer edits a side — the brief asked for the rule to be REPORTED, not invented. Proposed: the sides' own counts win once any side is confirmed; until then the quick-look total is the truth. |
| C8b | Window TYPE lives in two fields: `details.windowStyle` (the interior question, `lib/wizard/state.ts:266`, used by `merge.ts` for interior windows) and `exterior.windowType` (C8b). One job can carry both; nothing reads across. Fine today, a trap for a "same windows inside and out" shortcut. |
| C8b | "Cladding or panelling" and "not sure" both map to substrate `other` (`lib/wizard/exterior-quick-look.ts` `MATERIAL_TO_SUBSTRATE`) and the seed prices `other` at the weatherboard placeholder (`lib/wizard/starter.ts` `scaffoldCladdingCode`), flagged `exterior_material`. A panelling rate row would let the tick price honestly. |
| C8b | ⚑52 per-elevation painting allowances (allowances spec §8) still do not exist; only the per-side ACCESS hours do (`lib/wizard/exterior-allowances.ts`). The exterior range stays wider than the interior's until they do. |
| C8b | **C11 leftover, pre-existing on main:** the EXTERIOR-ONLY footer (`app/estimate/scope/SidesEditor.tsx:961` `sd-cta`, `:969` `cta-hint`) never got C11's human line — `humanLine` renders only in `ScopeEditor.tsx:1620` (`sc-stick`). `e2e/customer-journey/sides-editor.spec.ts:59` asserts `human-line` on that footer and fails on main (found by C8b's e2e run, not caused by it). One footer, one evaluator, for the sides editor too. |
| C13 | "What we'll do" on a warehouse reveal shows only the OFFICE rooms' interior lines: the floor's wall lines carry exterior substrate keys (`concrete`, `brick_unpainted`, `colorbond`) and `paintSystemsView` (`lib/wizard/systems-view.ts` `CARD_GROUPS`) has no exterior group, so walls, roller doors and personnel doors have no What-we'll-do line. A `commercialWhatWeDo` beside `exteriorWhatWeDo` is the fix. |
| C13 | §4.13's industrial PREP (sealer on bare precast, block filler on blockwork, wash + metal primer on sheeting) has no rows in the derivation table (`lib/pricing/systems.ts` has ONE exterior rule). C13 carries each as a crew note on the wall line and a `commercial_prep` flag (`lib/wizard/warehouse.ts` `WAREHOUSE_MATERIAL_ROW`), never hours. The rows are Tom's to add to the paint-systems table. |
| C13 | The engine prices a code missing on an area's own side at the other side's row (`lib/pricing/estimate.ts` `priceSurface`, C13). It is what lets an interior warehouse floor use the card's cladding and door rows. If Tom would rather the rate card carried Interior copies of the five industrial rows, that is a data migration and the fallback can go. |
| C13 | The roller-door row is `Garage Door (1 Car)` per door per face; a two-car-wide industrial roller door has no row of its own (`Garage Door (2 Car)` exists but the screen does not ask the width). Counted on site by the estimator; a width question is one tile away if it matters. |
| C13 | "Both faces only when the exterior element is ticked" (§4.13): an inside warehouse job never ticks an outside element, so roller doors always seed ONE face. The outside face arrives with C14's exterior brief, not from a form. |
