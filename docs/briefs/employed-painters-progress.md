# Employed painters — progress ledger

**Read this first in every session. Update it last.** One row per session from `claude-code-brief-employed-painters.md` §6. The row is the truth about what has shipped; the brief and `employed-painters-session-0.md` are the truth about what to build. If they disagree, say so in the session before writing code.

## Preflight — paste at the start of every session

    Read docs/briefs/employed-painters-progress.md. Take the last row whose
    status is not TODO. Then, before touching code, report in one block:
    1. git log --oneline <that row's SHA>..origin/main — what has landed since.
    2. supabase/migrations/ files newer than the row's last migration, and
       whether each has a row in public._prod_migrations (output the SELECT
       for Tom if you cannot read production).
    3. Settings keys the next session expects (employees_enabled,
       expenseThresholdCents), and which already exist.
    4. Any open branch touching the files the next session names in
       employed-painters-session-0.md §6.
    5. The contractor regression list (session-0 §7): confirm it is green on
       this branch before the session starts, and again before it ends.
    6. The next session to run, and anything in its block the repo has
       already done — shrink the block and say what you removed.
    Wait for confirmation before starting the session.

## Postflight — the last act of every session

    Add the row: session, status DONE / PARTIAL / BLOCKED, date, merge SHA,
    migrations written (and whether Tom has confirmed them live on test and
    prod), settings seeded, unit count before → after, adversarial money
    test result, contractor suite result, ⚑s touched, and one line of
    "what the next session should know". Commit the ledger in the same PR.

## The ledger

| Session | What | Status | Date | Merge SHA | Migrations written · live? | Settings seeded | Unit tests before → after | Money test | Contractor suite | ⚑ touched | Next session should know |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 0 | Read-only diagnostic | DONE — awaiting Tom's confirmation | 17 Sep 2026 | _(branch `feat/employed-painters`)_ | none (highest on main `20270152`) | none | 2585 baseline | not yet written | not run (no code changed) | A–J listed in session-0 §8 | Ten rulings in `employed-painters-session-0.md` §8; the defaults there apply if Tom says nothing. Session 1 does not start until Tom confirms the file map and the `wo_assignments` naming (⚑E). Two attention evaluators exist (⚑C) — employee items go to `lib/crm/work-queue.ts` only. |
| 1 | Identity + capability function + money contract | DONE | 17 Sep 2026 | _(branch `feat/employed-painters`)_ | `20270153000000_employment_type.sql` · **LIVE on TEST (read-back 7/7) and PROD (Tom pasted, 17 Sep)** | `employees_enabled` = `{"enabled": false}` (seeded by the migration) | 2585 → 2603 | **GREEN** — `e2e/employee-money.spec.ts`, 6/6 on the C1 stack | 51 passed; 2 red, neither this branch: `contractor-invoicing:299` (pre-existing on main, verified 16 Sep) and `offer-accept:45` (tray picks `.first()`, and the TEST project holds two `offered` work orders per ref `WO-OPT4CBC8` / `WO-OPTD5840`, both made 16 Sep by an earlier run — sweep them or the spec keeps failing) | C, D, G applied | **No `view=` contract existed on the painter side** — the contract is now RLS (`is_employee()` on five read policies) + `lib/painters/employeeView.ts` schemas + `lib/painters/money.ts` vocabulary. An employee today: signs in, sees Home/Jobs/Calendar/Help, no offers, no invoicing, no insurance nag, honest empty lists; `/portal/money` and `/portal/requests` 404. **`wo_events` meta carries `price_cents`** (variation events) so employees are excluded from it too — Session 3's job-detail RPC must return derived state, not events. **Contractors can still read `wo_variations.price_cents` and priced `wo_events` meta today** (customer money) — pre-existing, logged as ⚑J. `capabilities` now rides `ContractorSession`; pages branch on it, never on `employment_type`. The e2e money spec creates its own `pg.e2e.employee.*` account per run and deletes it — no new CI secret needed. |
| 2 | Assignments, Accept, lead painter, calendar, unavailability | DONE | 17 Sep 2026 | _(branch `feat/employed-painters`)_ | `20270154000000_wo_assignments.sql` · **LIVE on TEST (8/8) and PROD (Tom pasted, all true, 17 Sep)** | none | 2603 → 2603 (no pure helpers added; the logic is SQL + board) | **GREEN** — `e2e/employee-money.spec.ts` 6/6 re-run on this build | `wo-booking`, `wo-reschedule`, `wo-rls`, `wo-ticks`, `contractor-portal` all green on this build (37 passed); `e2e/employee-assign.spec.ts` 6/6 | A (default: confirm at assignment), E (`wo_assignments`), G (`start_date`), H (extend `contractor_unavailability`) | **The lead painter IS `work_orders.contractor_id`** — every existing who's-painting read stays correct; `set_lead_painter` moves it. `wo_visible_jobs` now unions assignments; `wo_painters(wo)` is the one read surface for bookings ∪ assignments (no mirror). The first `assign_job` on a job is the ONLY stage transition (`acceptance_mode: 'assigned'` on the event); later calls add painters. `assignJobAction` sends the customer confirmation after commit (idempotent per start date). Board: EMPLOYEE lanes, dashed = not yet seen, ★ lead, `1 OF 3`; detail sheet = Make lead / Add a painter / Take off. Drag on an assignment block = that painter's days only. Traps: `centreOf` in a drag spec must scroll the tray card BEFORE measuring the lane (the scroll moves the lanes — first run dropped on a contractor); a policy that subselects its own table recurses — use `wo_my_job_ids_as_contractor()`. Not done: gcal reconcile for employees (`lib/gcal/sync.ts` reads bookings only), leave/RDO request+approve flow (S7 — only the conflict check exists), the employee's own screens (S3). Test project: one leaked fixture estimate from a teardown `fetch failed` was cleaned by hand; `destroyLoopFixture` verified 0 rows. |
| 3 | Employee portal views: job list, calendar, job detail, Accept, time budget, Assigned label, can't-make-it | DONE | 17 Sep 2026 | _(branch `feat/employed-painters`)_ | `20270157000000_employee_job_views.sql` · **LIVE on TEST and PROD (Tom pasted 17 Sep, all true)** | none | 2603 → 2619 (`lib/crm/work-queue-employee.test.ts`, +offset-rule fix) | **GREEN** — `employee-money` 6/6 on this build | `wo-ticks`, `wo-updates`, `contractor-portal` green on this build; `e2e/employee-portal.spec.ts` 4/4 (list → Accept → start → before-photo → tick → sweep draft → can't-make-it → Reassign on Today) | — | **The employee never reads work_orders**: `employee_jobs(p_work_order_id)` (SECURITY DEFINER) returns the `view=employee` shape with the snapshot passed through `jsonb_strip_money()` — case-SENSITIVE regex (a case-blind one ate `gst_registered` and `accurate`). `lib/contractor/employeeJobs.ts` maps it onto `ContractorJob` (+`assignment`), so Jobs/Calendar/Home/job page are the same components branching on `capabilities`. `AssignmentCard` = Accept + can't-make-it (`assignment_cant_make_it` writes an event only; `employee_flag_state` tells the page a flag stands). Reassign item = `employee_reassign` in `lib/crm/work-queue.ts` (weight 32, followups, due day-before-start → sits in WAITING on a big queue: the e2e walks the follow-ups pages). `stageTitle(stage, mode)` derives "Assigned"; the PC page reads assignments to pick the label and "Ready to assign". Help: `employee` role in both enums, `helpReader()` routes employees to `employee.md` only; `docs/help/{scheduling,work-orders}/employee.md` written. Traps: the portal job page never rendered a stage badge (contractors either) — don't assert one there; the PC's daily draft is made by the sweep cron (`/api/cron/wo-sweep?force=1` with `CRON_SECRET`), not by `wo_draft_update`; Today paginates at 50 with Overdue first, so a waiting item is pages in. Not done: employee gcal (card hidden), `contractor_mark_wo_viewed` stamps only the lead (reads contractor_id), customer-portal rogue `RAIL` still says "Offer" (customer wording — Tom's call). |
| 4 | Variations, employee side + 4b crew can work the job | DONE — migrations await Tom on PROD | 17 Sep 2026 | _(branch `feat/employed-painters`)_ | `20270158000000_employee_variations.sql` + `20270159000000_crew_can_work.sql` · **LIVE on TEST (read-backs true) · NOT on prod — paste BOTH, in order, read back** | none | 2619 → 2619 | **GREEN** — `employee-money` 6/6 on this build | `wo-variations` 10/10, `variation-auto-release`, `wo-ticks`, `wo-photos`, `crew-leak` green on this build; `e2e/employee-variation.spec.ts` 4/4 (raised by the NON-lead crew member with a photo → priced → customer signs → "Variation approved" + hours + scope lines, no $, row already contractor_accepted with `variation_employee_applied` → declined = "Not going ahead" + note) | — | **Found and fixed (4b, ruling 6):** five painter RPCs (`wo_tick_surface`, `wo_record_photo`, `wo_tick_checklist_item`, `wo_answer_checklist_item`, `wo_add_note`), the bucket helper `wo_photo_access` and the scalar `wo_is_my_job_as_contractor` all tested `contractor_id` = the LEAD only, so a second employee could see the tick list and do nothing. `20270159` adds `wo_painter_on_job(wo, painter)` and re-creates those seven from their LIVE definitions (`pg_get_functiondef` on TEST, script-generated, one test swapped) — and makes `wo_photo_access` SECURITY DEFINER (it read `work_orders` with invoker rights → every employee upload refused by the bucket, not the app). `contractor_invoice_request` + `wo_contractor_accept_variation` deliberately stay lead-only. `app/api/wo/photos/route.ts` now asks `wo_is_my_job_as_contractor` instead of reading `work_orders`. Variations: `wo_raise_variation` widened the same way; `employee_variations(wo)` = money-free read (outcome + stripped priced lines); BEFORE UPDATE trigger `wo_variations_employee_autoapply` turns customer_approved → contractor_accepted on employee jobs (`wo_is_employee_job`), so the stage gate never waits on nobody; `Variations.tsx` has a `mode` prop (never forked). Traps: the customer's sign pad sits below the fold on a page with priced lines — `scrollIntoViewIfNeeded()` before `drawSignature`; fill the name BEFORE drawing. Session 3's e2e passed only because its employee was the lead. |
| 5 | Expenses only, settings by capability, the Employee tick box, the customer's lead, the crew's walkthrough | DONE — migration awaits Tom on PROD | 17 Sep 2026 | _(branch `feat/employed-painters`)_ | `20270161000000_employee_admin_expenses.sql` · **LIVE on TEST (read-back 6/6) · NOT on prod — paste + read back** | `employees_enabled` was seeded off in S1; the e2e turns it ON on the test project (leave it on there) | 2623 → 2623 | **GREEN** — `employee-money` 6/6 re-run on this build (its 404 test now expects the expenses page) | `contractor-expenses`, `contractor-portal`, `contractor-workcover`, `wo-walkthrough-v3` green on this build; `e2e/employee-money-tab.spec.ts` 5/5 ($140 personal claim → over-threshold → approve → Payables Employee reimbursements → mark paid back, never an invoice; company-card claim never on the list; settings = tickets only; tick box refused inline with an open offer, flips a clean contractor + their portal + logs the event, un-tick → not offerable; lead change moves the customer-facing name) | F (white_card + working_at_heights as doc kinds) | **`set_employment_type` is the only writer of the column**; refuses `open_offer:WO`, `active_assignment:WO`, `unpaid_invoice:CI`, `unpaid_expense`; flipping back calls `contractor_recompute_offerable`. `create_contractor_invite` gained `p_employment_type` (old 5-arg overload DROPPED to avoid PostgREST ambiguity — any caller passing 5 args still works via the default). `paid_with` is a SECOND write after `contractor_expense_submit` (`expense_set_paid_with`) — not atomic; a failure reads as personal, which surfaces on the office's list. Payables URL is `/invoicing?tab=pay` (not `payables`). Employees' reimbursements query uses `contractors!inner(employment_type)`. Four more RPCs widened from live defs (`contractor_expense_submit`, `expense_preapproval_request`, `wo_start_walkthrough_mode`, `wo_generate_report_draft`). Not done: a Settings UI for `employees_enabled` (S7 flips it by SQL/PR); customer-facing daily update never named a painter (nothing to change); `docs/help/self-invoicing/employee.md` written (Expenses). |
| 6 | Timesheets + job cost: Start/Finish day, PC approval, labour line on the job ledger, cost rates, payroll CSV, allocated-vs-actual | DONE — migration awaits Tom on PROD (and 20270161 before it) | 17 Sep 2026 | _(branch `feat/employed-painters`)_ | `20270163000000_timesheets.sql` · **LIVE on TEST (read-back 7/7) · NOT on prod — paste 20270161 first, then this, and read both back** | none (uses `employees_enabled` from S1) | 2623 → 2631 | **GREEN** — `employee-money` 8 denied paths now incl. `employee_cost_rates` + `job_costs`; `employee-timesheet` proves no `$`, no rate on the painter's pages or their own rows | **GREEN** — full session-0 §7 list + `contractor-workcover` + the five earlier employee specs, one C1 run: 219 passed, 1 failed = `contractor-invoicing:299` (pre-existing on main), 5 conditional skips, 4 did-not-run behind that red (same file); `offer-accept` green this run; `employee-timesheet` 5/5 in its own run | none new | **`job_cost_category` gains `'labour'`** (enum value — usable only after the paste commits; `timesheet_approve` uses it inside plpgsql so the same paste is fine). `timesheet_approve` REFUSES `no_rate` — a rate saved AFTER the work day does not price it (`employee_cost_rate_on` takes the latest `effective_from ≤ work_date`); the e2e dates its rate 2026-01-01. `timesheet_start()` with no job picks today's single assignment (`error:pick_job` on two — the job page names the job). Postgres has no `min(uuid)` — `(array_agg(...))[1]`. The office's wall-clock times → instants via `Intl` per day in `app/pc/timesheets/actions.ts` (never `+10:00`). The payroll CSV is `/pc/timesheets/export?from&to` (staff, 404 otherwise, 503 on a failed read). The PC job page's GP now subtracts approved labour; the invoicing job page lists the line under Other trades & costs (category `labour`). `wo_visible_jobs` unchanged — employees read their own `timesheet_entries` via `timesheet_entries_own`. Not done: the >24h-awaiting-approval work item (S7), bonus report (later), gcal for employees (still parked). |
