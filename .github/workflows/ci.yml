# CI — the gate the August 2026 audit found missing entirely (A1-05).
#
# Two jobs:
#   gate  typecheck + lint + unit tests. No secrets, runs everywhere, fast.
#   e2e   the real browser against the TEST Supabase project. Needs secrets.
#
# `gate` is the one to mark "required" in branch protection today. Add `e2e`
# as required once its secrets are in (see the job's guard step).
name: CI

on:
  push:
    branches: ["**"]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gate:
    name: typecheck · lint · unit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci

      # Next GENERATES the route/layout types (LayoutProps, PageProps) into
      # .next/types. A clean checkout has none, so tsc fails with "Cannot find
      # name 'LayoutProps'" — which is exactly how CI run #1 failed while the
      # same command passed locally, on a working tree that still had .next
      # from an earlier build. typegen does it without a full build.
      - name: Generate Next route types
        run: npx next typegen

      - name: Typecheck
        run: npx tsc --noEmit

      # CLAUDE.md: "lint and typecheck must pass before any commit". Errors are
      # zero and must stay zero. The warning cap is a RATCHET, not a target —
      # it was 23 when CI landed (F1). Lower it when you clear some; never
      # raise it. Raising it is how 23 becomes 200.
      - name: Lint (0 errors, warnings ratcheted at 23)
        run: npx eslint --max-warnings 23

      - name: Unit tests
        run: npm test

      # The money paths are meant to be mutation-tested (audit P6). This is the
      # cheap standing version: break a pricing constant, prove the suite
      # notices. If this step ever PASSES, the pricing suite has stopped
      # testing pricing.
      - name: Mutation canary — pricing must be defended
        run: |
          cp lib/pricing/engine.ts /tmp/engine.bak
          sed -i 's/1 + 0\.75 \* (coats - 1)/1 + 0.70 * (coats - 1)/' lib/pricing/engine.ts
          if npx vitest run lib/pricing >/dev/null 2>&1; then
            cp /tmp/engine.bak lib/pricing/engine.ts
            echo "::error::Mutating the marginal-coat rule did not fail any test."
            echo "The pricing suite is not testing what it claims. See docs/audits/audit-2026-08-full.md (P6)."
            exit 1
          fi
          cp /tmp/engine.bak lib/pricing/engine.ts
          echo "Pricing mutation was caught — suite is live."

  e2e:
    name: e2e (test project)
    runs-on: ubuntu-latest
    needs: gate
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci

      # Fail with instructions rather than skipping. A skipped e2e job that
      # reports green is exactly the failure mode A1-06 describes: 160
      # conditional skips meant a passing run could assert almost nothing.
      #
      # These secrets carry the TEST project's values under the same names the
      # app uses. That is convenient but means a future deploy workflow adding
      # PRODUCTION values under these names would silently point the e2e suite
      # — which mutates and deletes rows — at production. e2e/global-setup.ts
      # refuses the known production ref outright for exactly this reason; do
      # not remove that check on the grounds that "the tripwire reads
      # .env.local", because .env.local does not exist in CI.
      - name: Check the e2e secrets are configured
        env:
          URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
        run: |
          if [ -z "$URL" ]; then
            echo "::error::e2e secrets are not configured, so this job cannot run."
            echo "Add these repository secrets — TEST project only, never production:"
            echo "  NEXT_PUBLIC_SUPABASE_URL  NEXT_PUBLIC_SUPABASE_ANON_KEY  SUPABASE_SERVICE_ROLE_KEY"
            echo "  E2E_STAFF_EMAIL  E2E_STAFF_PASSWORD"
            echo "  E2E_CONTRACTOR_EMAIL  E2E_CONTRACTOR_PASSWORD"
            echo "  E2E_CUSTOMER_EMAIL  E2E_CUSTOMER_PASSWORD"
            echo "Values are in .env.test.local. Until then, remove 'e2e' from"
            echo "required checks — do NOT make this job skip silently."
            exit 1
          fi

      - run: npx playwright install --with-deps chromium

      - name: Build (test stack env baked in, as a deploy would)
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          NEXT_PUBLIC_SITE_URL: http://localhost:3101
        run: npm run build

      # SCOPE, and why it is not the whole suite.
      #
      # CLAUDE.md's e2e-first law says the customer-journey suite must be green
      # before any merge, and it is NOT in this list. It cannot be: the C1 test
      # project was provisioned for the invoicing/money suite (run-e2e.sh even
      # defaults to stripe-live.spec.ts), and the wizard's reference data was
      # never seeded there —
      #
      #   wizard_public setting   ABSENT
      #   sundries                0 rows
      #   measurement_units       0 rows
      #   room_type_defaults      0 rows
      #   room_type_scope_rules   0 rows
      #   defect_prep_rates       0 rows
      #   rate_items              3 rows
      #
      # Run #6 proved it: account-rls passed 7/7, then batch-edits.spec.ts sat
      # at a 3-minute timeout per test because the wizard cannot render without
      # that data. 49 tests on one worker would have burned an hour to tell us
      # something a row count tells us instantly.
      #
      # Listed here are the specs VERIFIED to pass on the C1 stack. Widening
      # this list without running the specs there first is how run #6 happened.
      # Seeding the test project for the wizard is finding F1-02.
      - name: Role RLS · ledger parity · settings atomicity
        env:
          CI: "1"
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          E2E_STAFF_EMAIL: ${{ secrets.E2E_STAFF_EMAIL }}
          E2E_STAFF_PASSWORD: ${{ secrets.E2E_STAFF_PASSWORD }}
          E2E_CONTRACTOR_EMAIL: ${{ secrets.E2E_CONTRACTOR_EMAIL }}
          E2E_CONTRACTOR_PASSWORD: ${{ secrets.E2E_CONTRACTOR_PASSWORD }}
          E2E_CUSTOMER_EMAIL: ${{ secrets.E2E_CUSTOMER_EMAIL }}
          E2E_CUSTOMER_PASSWORD: ${{ secrets.E2E_CUSTOMER_PASSWORD }}
          E2E_BASE_URL: http://localhost:3101
          NEXT_PUBLIC_SITE_URL: http://localhost:3101
        # CI=1 makes e2e/global-setup.ts turn a missing credential into a failed
        # run instead of a silent skip, and it still refuses production.
        run: |
          npx next start -p 3101 &
          for i in $(seq 1 60); do curl -sf -o /dev/null http://localhost:3101/login && break; sleep 1; done
          npx playwright test e2e/wo-rls.spec.ts e2e/account-rls.spec.ts e2e/ledger-parity.spec.ts e2e/settings-save-atomic.spec.ts

      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-traces
          path: test-results/
          retention-days: 7
