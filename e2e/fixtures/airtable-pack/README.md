# Synthetic Airtable pack for e2e

Five made-up customers (pg.e2e.airtable.*@example.com) and one signed job in the shapes
`docs/imports/airtable-crm-import/transform.py` writes. No real person is in here. Used by
`e2e/airtable-import.spec.ts`, which runs both loaders against it and purges what they wrote.
